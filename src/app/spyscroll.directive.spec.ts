import { SpyscrollDirective } from './spyscroll.directive';

describe('SpyscrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SpyscrollDirective();
    expect(directive).toBeTruthy();
  });
});
